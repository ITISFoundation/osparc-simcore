components:
  schemas:
    ServicesEnveloped:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: './node-meta-v0.0.1-converted.yaml'
        error:
          nullable: true
          default: null
          