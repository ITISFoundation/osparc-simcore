#
# Specifies dependencies required to run 's3wrapper'
#

# frozen specs
-r _base.txt

# testing
coverage
pytest
pytest-cov
pytest-docker
pytest-runner

# mockups/fixtures
requests

# tools for CI
pylint==2.5.0 # 2.5.3 fails to run in parallel
coveralls
