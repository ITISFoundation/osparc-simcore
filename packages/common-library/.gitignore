# Translations
#  Only the .po files (human translations) are committed. That's the right split:
#  Build artifacts (.pot, .mo, _partials/) → gitignored, regenerated by make all
#  Translations (.po) → committed, versioned, reviewed in PRs
*.mo
*.pot
