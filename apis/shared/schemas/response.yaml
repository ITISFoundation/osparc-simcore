Envelope:
  type: object
  properties:
    data:
      type: string
      nullable: true
      default: null
    error:
      oneOf:
        - type: string
          nullable: true
          default: null
        - $ref: "./error.yaml/#Error"

SomethingEnveloped:
  description: blah
  allOf:
    - $ref: "#/Envelope"
    - type: object
      properties:
        data:
          