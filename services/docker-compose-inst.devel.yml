# Overrides docker-compose-inst.yml for development mode
# Usage:
#   docker-compose -f docker-compose.yml -f docker-compose.devel.yml ...
#
version: '3.7'
services:
  maintenance:
    image: services_maintenance:dev
    volumes:
      - ./maintenance/notebooks:/home/jovyan/work
