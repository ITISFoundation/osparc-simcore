#
# Specifies third-party dependencies for 'services/director-v2/src'
#
# NOTE: ALL version constraints MUST be commented

# fastapi and extensions
fastapi[all]
async-exit-stack  # not needed when python>=3.7
async-generator   # not needed when python>=3.7

# data models
pydantic[dotenv]


# docker daemon
aiodocker

# other
tenacity

# Limiting constraint from pytest-docker==0.8.0->-r requirements/_test.in (line 18))
attrs<20,>=19
