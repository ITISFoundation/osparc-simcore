--constraint ../../../requirements/constraints.txt

--constraint _base.txt
--constraint _test.txt

--requirement ../../../requirements/devenv.txt

# basic dev tools
watchdog[watchmedo]

# memory profiler used to hunt leaks via 'memray run --native -m uvicorn ...' (boot.sh)
memray
