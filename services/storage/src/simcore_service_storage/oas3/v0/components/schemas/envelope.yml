Envelope:
  description: default envelope
  required:
    - data
    - error
  type: object
  properties:
    data:
      nullable: true
      default: null
    error:
      $ref: "./error.yml#/ErrorType"
      nullable: true
      default: null


# TODO: still not sure how to reuse this ...
