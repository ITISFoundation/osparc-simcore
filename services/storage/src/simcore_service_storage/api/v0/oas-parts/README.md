# OpenApi Specification Parts [oas-parts]

  - User changes OAS in this folder
  - `cd api; make` will produce final `openapi.yaml`
  - Folder cannot be named `parts` because it used in packaging and is in the gitignore list
