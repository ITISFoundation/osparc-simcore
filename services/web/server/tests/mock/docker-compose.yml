version: '3.4'
services:
  db:
    image: postgres:10
    #volumes: TODO: make db persistent
    #  - '.:/home/scu/client'
    ports:
      - '5432:5432'
  adminer:
    image: adminer
    ports:
      - 18080:8080
    depends_on:
      - db
