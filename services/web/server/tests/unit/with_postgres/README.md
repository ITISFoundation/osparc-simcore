# Unit testing with a postgres service fixture

Testing session with a postgres database (see [docker-compose file](docker-compose.yml))

- Use [docker-compose-debug file](docker-compose.debug.yml) for development
