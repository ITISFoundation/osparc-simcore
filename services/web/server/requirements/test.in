# Specifies dependencies required for services/web/server/tests
#
# NOTE: order defines priority

# testing
pytest
pytest-aiohttp  # incompatible with pytest-asyncio. See https://github.com/pytest-dev/pytest-asyncio/issues/76
pytest-cov
pytest-docker
pytest-mock
pytest-runner
coveralls
codecov

# fixtures
Faker
openapi-spec-validator # TODO: this library is limiting jsonschema<3 (see base.in)

# linter
pylint~=2.0
