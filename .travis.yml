matrix:
  include:
    - language: node_js
      node_js:
        - "8"

      cache:
        directories:
          - services/node_modules
          - services/web-client/contrib

      install:
        - pushd services
        - npm install
        - npm run installQxContrib
        - popd

      script:
        - npm run linter

    - language: python
      python:
        - "3.6"
      install:
        - pip3 install pylint
      before_script:
        - pylint --version
      script:
        - make pylint
