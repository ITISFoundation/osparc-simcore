name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v1
    - name: build images
      run: |
        ./ci/travis/build/test-images before_script
        ./ci/travis/build/test-images script    
